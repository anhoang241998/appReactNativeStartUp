'use strict';

import React from 'react';

import {
  View,
} from 'react-native';

const Wifi = (props) => {
	return (
      	<View style={styles.container}/>
    );
}

const styles = {
	container: {
		flex: 1,
		backgroundColor: 'green',
	}
}

export default Wifi;