'use strict';

import React from 'react';

import {
  View,
} from 'react-native';

const SmartConfig = (props) => {
	return (
      	<View style={styles.container}/>
    );
}

const styles = {
	container: {
		flex: 1,
		backgroundColor: 'yellow',
	}
}

export default SmartConfig;